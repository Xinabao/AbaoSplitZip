name: Build Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact_name: AbaoZip-Windows
            exe_name: AbaoZip.exe
          - os: ubuntu-20.04
            artifact_name: AbaoZip-Linux
            exe_name: AbaoZip
          - os: macos-latest
            artifact_name: AbaoZip-macOS
            exe_name: AbaoZip

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Linux libs (if Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxcb-cursor0

      - name: Build with PyInstaller
        run: |
          python -m PyInstaller build.spec --clean --noconfirm

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.exe_name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare Release Assets
        run: |
          mkdir -p release
          
          # Windows
          cp artifacts/AbaoZip-Windows/AbaoZip.exe release/
          cd release && zip AbaoZip-Windows.zip AbaoZip.exe && rm AbaoZip.exe && cd ..
          
          # Linux
          cp artifacts/AbaoZip-Linux/AbaoZip release/
          cd release && chmod +x AbaoZip && tar czf AbaoZip-Linux.tar.gz AbaoZip && rm AbaoZip && cd ..
          
          # macOS
          cp artifacts/AbaoZip-macOS/AbaoZip release/
          cd release && chmod +x AbaoZip && tar czf AbaoZip-macOS.tar.gz AbaoZip && rm AbaoZip && cd ..

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/AbaoZip-Windows.zip
            release/AbaoZip-Linux.tar.gz
            release/AbaoZip-macOS.tar.gz
          generate_release_notes: true
